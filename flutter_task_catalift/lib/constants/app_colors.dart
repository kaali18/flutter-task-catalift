import 'package:flutter/material.dart';

class AppColors {
  // Primary colors
  static const Color primary = Color(0xFF0A0E5C);
  static const Color primaryLight = Color(0xFF303F9F);
  
  // Background colors
  static const Color background = Colors.white;
  static const Color surfaceLight = Color(0xFFF5F5F5);
  static const Color tabBackground = Color(0xFFE8EAF6);
  
  // Text colors
  static const Color textPrimary = Color(0xFF0A0E5C);
  static const Color textSecondary = Colors.grey;
  
  // Status colors
  static const Color highCompatibility = Color(0xFF388E3C);
  static const Color mediumCompatibility = Color(0xFFFFB300);
  static const Color sectorTag = Color(0xFFFF9800);
  
  // Rating
  static const Color starColor = Colors.amber;
  
  // Shadows
  static Color shadowColor = Colors.black.withOpacity(0.05);
}